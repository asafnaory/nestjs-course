export const jwtConstants = {
    secret: 'supercalifragilisticexpialidocious',
    expiresIn: '1h',
  };
// expiresIn - default is a number representing milliseconds. optional string units - m / h / d `ms` by Vercel is used under the hood so any of its formats will work check out the full list of string examples :